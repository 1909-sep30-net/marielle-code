@{
    //all views start here
    Layout = "_Layout";
    //every view can have one layout, that layout typically has the outer structure of your html file, and at some point it calls @RenderBody()
    //that is where the view will go 
}
